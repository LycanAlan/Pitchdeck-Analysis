# Models
google-genai>=1.0.0
langgraph>=0.2.60
langchain-core>=0.3.0          # required by langgraph's runtime; no langchain chains are used

# Embeddings and re-ranking run on ONNX Runtime, not PyTorch. Same MiniLM
# weights and identical outputs (verified cosine 1.000000 against
# sentence-transformers), but ~298 MB resident instead of ~523 MB, which is what
# keeps the service inside a 512 MB free-tier container. Dropping torch also
# removes roughly 2 GB from the install.
fastembed>=0.4.0

# Retrieval
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
numpy>=1.26

# Ingestion
pymupdf>=1.24.0

# Evaluation / reporting
pandas>=2.2

# Serving
fastapi>=0.115.0
python-multipart>=0.0.9        # FastAPI needs it to parse the /ingest upload
uvicorn[standard]>=0.30.0
streamlit>=1.38.0
requests>=2.32.0

# Shared
pydantic>=2.8
python-dotenv>=1.0.1
